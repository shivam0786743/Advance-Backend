events {}

http {
    upstream server {
        server server1:6000;
        server server2:6000;
        server server3:6000;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://server;
        }
    }
}
